# Base URL of the backend API.
# Leave empty in development — Vite proxies /api to http://localhost:5000 (see vite.config.js).
# In production set it to your deployed backend, e.g. https://your-api.onrender.com
VITE_API_URL=
